# PostgreSQL connection string
DATABASE_URL="postgresql://sap:sap123@localhost:5433/sap_beta?schema=public"

# App URL (used for Socket.IO client connection)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Code expiration in hours (default: 24)
ANALYSIS_CODE_EXPIRY_HOURS=24

# Node environment
NODE_ENV=development
